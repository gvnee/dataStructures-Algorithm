#include<bits/stdc++.h>
using namespace std;

#define ll long long
#define sz(x) int((x).size())
#define debug(x) cerr<<#x<<": "<<(x)<<"\n"

bool cmp(ll a, ll b){
  return a>b;
}

const int mxn = 1000000001;
vector<ll> dp[mxn];
void f(){
  ll n, k;
  cin>>n>>k;
  ll arr[n];
  for(int i = 0;i<n;i++){
    cin>>arr[i];
  }
}

int main(){
  ios_base::sync_with_stdio(0); cin.tie(0);
  int t = 1;
  // cin>>t;
  while(t--) f();
  return 0;
}